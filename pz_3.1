def chetv(x, y):
    if ((x < 0) and (y > 0)):
        res = True
        print(res)
        return res
    if ((x < 0) and (y <= 0)):
        res = True
        print(res)
        return res
 
 
if __name__ == '__main__':
    assert chetv(-2, 1)
    assert chetv(-5, -5)
